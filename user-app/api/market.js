import apiClient from "./client";
const endpoint = "/user/api/v1/market";

// const getStates = () => apiClient.post(endpoint + "/get-states", {});
// const getMarkets = (state: any) => apiClient.post(endpoint + "/get-markets", state);
// const getItems = () => apiClient.post(endpoint + "/fetch-items", {});

export default {
  // getStates,
  // getMarkets,
  // getItems
};
